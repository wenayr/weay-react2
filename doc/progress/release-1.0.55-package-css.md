# v1.0.55 package CSS

Goal: publish a reproducible `wenay-react2` archive containing the CSS that
`lib/common/api.js` imports.

- [x] Confirmed 1.0.53 and 1.0.54 archives omit `lib/style`.
- [x] Changed pack flow to run the full build.
- [x] Build and inspect the 1.0.55 archive: `menuRight.css`, `style.css`, and
  `tokens.css` are present under `package/lib/style/`.

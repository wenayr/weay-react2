# v1.0.72

Date: 2026-08-03

- Compatibility-only release: the minimum `wenay-common2` version is now
  `2.6.1`; no React public API changed.
- Adopted upstream correctness fixes for exact replay-journal memory bounds,
  Lazy Line stale-cursor mirror cleanup, cross-lifetime cursor rejection, and
  reliable completion of the initial progressive fill.
- Verification: strict TypeScript checks, Jest, clean package build, bundle
  probe, packed-artifact inspection, and registry metadata checks.

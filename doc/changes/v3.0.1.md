# v3.0.1

Date: 2026-09-08

- Changed: updated `wenay-common2` from 2.13.0 to 2.16.0 in dependencies and peer dependencies.
- React-facing impact: existing hooks remain compatible. The corrected RPC Replay inference lets
  the QA stand and reconnect integration test remove their `unknown` casts; no public API or
  runtime adaptation is required. The new upstream `Observe.storeExternal` helper remains an
  optional lower-level bridge for direct `useSyncExternalStore` consumers.
- Verification: `npm run typecheck`, all 62 Jest suites / 313 tests, and the full package build
  with generated declaration, entrypoint, bundle-boundary, and packed-consumer probes.

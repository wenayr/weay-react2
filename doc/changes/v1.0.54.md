# v1.0.54

Date: 2026-07-15

- Changed: `SettingsDialog` now also restores the selected section from its layout state.
- Safety: if an application update removes that section, the dialog falls back to its declared default section.

Verification: SettingsDialog Jest tests; library build; client TypeScript checks.

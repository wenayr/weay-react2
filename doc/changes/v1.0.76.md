# v1.0.76

Date: 2026-08-04

- Updated: the minimum `wenay-common2` version is now `2.9.0`; existing React
  APIs remain compatible.
- Available upstream: the opt-in `openFsSpillJournal` RAM-first replay
  retention mode and guidance for resumable flow streams.
- No behavior changes apply unless the new server-side module is used.
- Verification: strict TypeScript checks, Jest, clean package build, bundle
  probe, packed-artifact inspection, and registry metadata checks.

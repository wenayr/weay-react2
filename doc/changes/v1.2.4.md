# v1.2.4

Date: 2026-08-26

- Changed: dependency bump only, `wenay-common2` 2.13.0 (pure bug-fix release, unchanged type surface and peer requirements). Two of its fixes land under code paths this library uses: the audio-source generation guard behind `useMediaSource`'s stop/restart on device change, and the store-lazy-line `__proto__` key that `useReplay` mirrors.
- Changed: range mirrored in `dependencies` and `peerDependencies`; 2.13.0 appended to `minimumReleaseAgeExclude` in `pnpm-workspace.yaml`.
- Verification: 57 Jest suites / 266 tests, both typecheck configs, entrypoint and packed-consumer probes against the rebuilt dist.

# v1.0.73

Date: 2026-08-03

- Compatibility-only release: the minimum `wenay-common2` version is now
  `2.6.2`; no React public API changed.
- Adopted upstream Lazy Line convergence fix for a stale cursor received partway
  through the initial progressive fill, preventing deleted keys from surviving
  a restarted host pass.
- Verification: strict TypeScript checks, Jest, clean package build, bundle
  probe, packed-artifact inspection, and registry metadata checks.

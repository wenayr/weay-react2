# v1.0.75

Date: 2026-08-04

- Updated: the minimum `wenay-common2` version is now `2.8.0`; existing React
  APIs remain compatible.
- Available upstream: opt-in flow-paced streaming callbacks via `flowCallback`,
  including negotiated credit-based backpressure and a local watermark fallback.
- Verification: strict TypeScript checks, Jest, clean package build, bundle
  probe, packed-artifact inspection, and registry metadata checks.

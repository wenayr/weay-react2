# v1.0.63

Date: 2026-07-22

- Fixed: parameter commentary tooltips no longer capture pointer input, so
  moving the pointer over a tooltip does not break the owning row's hover state
  or make the hint flicker.
- Published: the fix as `wenay-react2@1.0.63`; its common2 dependency remained
  on the 1.x line. This entry restores the local release catalog for the
  already-published package.
- Verification: compared the published 1.0.62 and 1.0.63 npm artifacts and
  confirmed that the tooltip pointer-events change is the only source delta.

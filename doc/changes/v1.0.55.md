# v1.0.55

Date: 2026-07-15

- Fixed: the packaged `lib/style` directory is now built before packing, so
  `lib/common/api.js` can resolve its `menuRight.css` and `style.css` imports.
- Safety: `npm run pack` now runs the full library build instead of preparing a
  partial `dist` directory.

Verification: package archive contains `lib/style/menuRight.css`,
`lib/style/style.css`, and `lib/style/tokens.css`; a clean Vite/esbuild build
resolves the imports from `lib/common/api.js`.

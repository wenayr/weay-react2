# v1.0.56

Date: 2026-07-15

- Added: legacy `createRightClickMenu().MenuR` accepts `captureGlobal` to open
  its current menu for right clicks outside the wrapper, including portal and
  floating-window content. Default wrapper-only behavior is unchanged.
- Verified: document listeners respect `statusOn`, suppress the native context
  menu only when enabled, and are removed on unmount.

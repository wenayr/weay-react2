# v1.0.74

Date: 2026-08-04

- Updated: the minimum `wenay-common2` version is now `2.7.0`; existing React
  APIs remain compatible.
- Available upstream: static key selection for Store Lazy Lines and an opt-in
  byte budget for filesystem replay journals.
- Adopted upstream Lazy Line hardening for root-swap tombstones and tombstone
  expiry on reads.
- Verification: strict TypeScript checks, Jest, clean package build, bundle
  probe, packed-artifact inspection, and registry metadata checks.

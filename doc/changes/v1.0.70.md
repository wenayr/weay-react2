# v1.0.70

Date: 2026-07-30

- Updated: the minimum `wenay-common2` version is now `2.3.0`.
- Added upstream: selective Store Replay views with bounded snapshot chunks,
  resumable view cursors, and atomic client-side commits.
- Improved upstream: exact array-slot replay patches and lower CPU, heap, and
  wire overhead for large Store Replay workloads.
- Updated: `@swc/core` is now `1.15.47`.
- Compatibility: the common2 changes are additive; existing React replay hooks
  keep their public API and pass without substitutions.
- Verification: strict TypeScript checks, Jest, clean package build, bundle
  probe, packed-artifact inspection, and registry metadata checks.

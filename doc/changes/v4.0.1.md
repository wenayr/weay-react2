# v4.0.1 — wenay-common2 3.1.1

Date: 2026-09-24

- Changed: `wenay-common2` dependency and peer `^3.0.0` → `^3.1.1`; release-age exclusion in
  `pnpm-workspace.yaml`, lockfile updated with the global pnpm 11.14. `wenay-exchange` stays
  1.0.0 (its peer `wenay-common2 >=3.0.0` is satisfied).
- Reviewed: 3.0.1 (security and correctness), 3.1.0 (performance), 3.1.1 (Store engine memory).
  No new common2 API needs a React binding; hook signatures and imports are unchanged.
- React impact, inherited: leaf `useStoreNode` / `useStoreKeys` subscriptions survive a parent
  deleted and recreated in one drain window; lazy-line mirrors hold detached values; a replay
  consumer that throws during catch-up releases its live subscription; Store nodes take about
  200 B instead of 1.8 KB. Behaviour to know (3.1.0): leaf subscriptions do not re-read raw writes
  that bypass the Store proxy. Details: rare doc, "common2 3.1.1".
- Size: `./logs` 61.1 → 61.8 KB and `./params` 68.2 → 68.8 KB gzip (codec fast paths in the common2
  client barrel); every other entry unchanged.
- Verification: both TypeScript configs; Jest 74 suites / 361 tests; migration CLI 17 tests; full
  build with the entrypoint and packed-consumer probes; bundle boundary probe. QA stand against the
  Vite backend on 3.1.1: card 55 (chunked keyframe reload, 24/24 parts, live stock after load),
  card 56 (owned client, a task, account switch), card 42 (presence edges on disconnect and
  reconnect), and a `#video-calls` room created, chatted in and closed; no console or server errors.
